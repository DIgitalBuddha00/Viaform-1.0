import type {FigRulesetLevel,FigVault} from "./types";
export type FigVaultContext="TEAM_OR_AA"|"APPARATUS_QUALIFICATION"|"APPARATUS_FINAL";
export function figLevel(value?:string|null):FigRulesetLevel{return String(value||"").toUpperCase().includes("JUNIOR")?"JUNIOR":"SENIOR"}
export function validateVaultPair(level:FigRulesetLevel,first:FigVault|null,second:FigVault|null){const issues:string[]=[];if(!first)issues.push("Choose a primary vault.");if(!second)issues.push("Choose a second vault.");if(first&&second){if(first.id===second.id)issues.push("The same performed vault cannot be repeated.");else if(level==="JUNIOR"&&first.officialNumber===second.officialNumber)issues.push("Junior apparatus qualification and final require two different vault numbers.");}return {valid:issues.length===0,issues,averageD:first&&second?(first.dValue+second.dValue)/2:null};}
export function validateSeniorVaultForContext(context:FigVaultContext,first:FigVault|null,second:FigVault|null){const issues:string[]=[];const requiresPair=context==="APPARATUS_QUALIFICATION"||context==="APPARATUS_FINAL";if(!first)issues.push("Choose a primary vault.");if(requiresPair&&!second)issues.push("Choose a second vault.");if(requiresPair&&first&&second){if(first.id===second.id)issues.push("The same performed vault cannot be repeated.");else if(first.group===second.group)issues.push("Senior apparatus qualification and final require two vaults from different groups.");}return {valid:issues.length===0,issues};}
export function seniorVaultDirectionBonus(first:FigVault|null,second:FigVault|null,performedWithoutFalls:boolean){if(!first||!second||!performedWithoutFalls)return 0;const directions=new Set([first.secondFlightDirection,second.secondFlightDirection]);return directions.has("FORWARD")&&directions.has("BACKWARD")?0.2:0;}
export function allAroundVaultScore(firstVaultFinal:number){return firstVaultFinal}
export function vaultEventScore(firstFinal:number,secondFinal:number){return (firstFinal+secondFinal)/2}
