"use server";
import { revalidatePath } from "next/cache";
import { redirect } from "next/navigation";
import { requireAuthContext } from "@/app/lib/auth";
import { prisma } from "@/app/lib/prisma";
const v=(d:FormData,k:string)=>String(d.get(k)??"").trim();
async function manager(){const c=await requireAuthContext();if(!c.access.canManageProgrammesAndMethodology)redirect("/rulesets?error=permission");return c;}
export async function enableRulesetProgram(d:FormData){const c=await manager(),programId=v(d,"programId");const p=await prisma.rulesetProgram.findFirst({where:{id:programId,status:"ACTIVE"}});if(!p)return;await prisma.organisationRulesetAssignment.upsert({where:{organisationId_programId:{organisationId:c.organisation.id,programId}},create:{organisationId:c.organisation.id,programId},update:{}});revalidatePath("/rulesets");}
export async function disableRulesetProgram(d:FormData){const c=await manager(),programId=v(d,"programId");await prisma.$transaction([prisma.gymnastRulesetAssignment.deleteMany({where:{programId,gymnast:{organisationId:c.organisation.id}}}),prisma.organisationRulesetAssignment.deleteMany({where:{organisationId:c.organisation.id,programId}})]);revalidatePath("/rulesets");}
export async function assignGymnastRuleset(d:FormData){const c=await manager(),gymnastId=v(d,"gymnastId"),programId=v(d,"programId"),levelId=v(d,"levelId");const gymnast=await prisma.gymnast.findFirst({where:{id:gymnastId,organisationId:c.organisation.id}}),enabled=await prisma.organisationRulesetAssignment.findUnique({where:{organisationId_programId:{organisationId:c.organisation.id,programId}}}),level=levelId?await prisma.rulesetLevel.findFirst({where:{id:levelId,programId,status:"ACTIVE"}}):null;if(!gymnast||!enabled||(levelId&&!level))return;await prisma.gymnastRulesetAssignment.upsert({where:{gymnastId},create:{gymnastId,programId,levelId:level?.id},update:{programId,levelId:level?.id??null}});revalidatePath("/gymnasts/"+gymnastId);}
export async function clearGymnastRuleset(d:FormData){const c=await manager(),gymnastId=v(d,"gymnastId");const gymnast=await prisma.gymnast.findFirst({where:{id:gymnastId,organisationId:c.organisation.id}});if(!gymnast)return;await prisma.gymnastRulesetAssignment.deleteMany({where:{gymnastId}});revalidatePath("/gymnasts/"+gymnastId);}
